apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
spec:
  # selector:
  # matchLabels:
  #   app: frontend
  template:
  # metadata:
  #   labels:
  #     app: frontend
  spec:
    containers:
      - name: app
        image: foo/bar:latest
        ports:
          - name: http
            containerPort: 8080
            protocol: TCP
